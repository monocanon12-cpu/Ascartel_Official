name: Keep Backend Alive

on:
  schedule:
    - cron: '*/14 * * * *'  # Toutes les 14 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          curl -f https://ascartel-backend.onrender.com/api/articles || exit 0
          echo "✅ Backend is alive"
